/*
 * BiddingDrive Messages
 *
 * This contains all the text for the BiddingDrive container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.BiddingDrive";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BiddingDrive container!"
  }
});
