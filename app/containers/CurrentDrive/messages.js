/*
 * CurrentDrive Messages
 *
 * This contains all the text for the CurrentDrive container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.CurrentDrive";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CurrentDrive container!"
  }
});
