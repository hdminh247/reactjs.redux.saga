/*
 * PromotionDetail Messages
 *
 * This contains all the text for the PromotionDetail container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.PromotionDetail";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PromotionDetail container!"
  }
});
