// import { fromJS } from 'immutable';
// import { selectChangeLocaleLoadingDomain } from '../selectors';

describe("selectChangeLocaleLoadingDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
