/*
 * MyDrive Messages
 *
 * This contains all the text for the MyDrive container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.MyDrive";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the MyDrive container!"
  }
});
