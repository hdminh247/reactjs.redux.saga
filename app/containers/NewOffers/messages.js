/*
 * NewOffers Messages
 *
 * This contains all the text for the NewOffers container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.NewOffers";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the NewOffers container!"
  }
});
