/*
 * PromotionList Messages
 *
 * This contains all the text for the PromotionList container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.PromotionList";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PromotionList container!"
  }
});
