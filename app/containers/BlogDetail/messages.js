/*
 * BlogDetail Messages
 *
 * This contains all the text for the BlogDetail container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.BlogDetail";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BlogDetail container!"
  }
});
