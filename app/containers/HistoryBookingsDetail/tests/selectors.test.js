// import { fromJS } from 'immutable';
// import { selectHistoryBookingsDetailDomain } from '../selectors';

describe("selectHistoryBookingsDetailDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
