/*
 * MyBooking Messages
 *
 * This contains all the text for the MyBooking container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.MyBooking";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the MyBooking container!"
  }
});
