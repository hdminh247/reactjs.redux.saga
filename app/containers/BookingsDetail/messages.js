/*
 * BookingsDetail Messages
 *
 * This contains all the text for the BookingsDetail container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.BookingsDetail";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BookingsDetail container!"
  }
});
