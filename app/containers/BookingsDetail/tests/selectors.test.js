// import { fromJS } from 'immutable';
// import { selectCurrentBookingsDetailDomain } from '../selectors';

describe("selectCurrentBookingsDetailDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
