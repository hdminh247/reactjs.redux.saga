// import { fromJS } from 'immutable';
// import { selectFeaturedDestinationPageDomain } from '../selectors';

describe("selectFeaturedDestinationPageDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
