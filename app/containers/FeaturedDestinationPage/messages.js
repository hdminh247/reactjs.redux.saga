/*
 * FeaturedDestinationPage Messages
 *
 * This contains all the text for the FeaturedDestinationPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.FeaturedDestinationPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the FeaturedDestinationPage container!"
  }
});
