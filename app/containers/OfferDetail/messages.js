/*
 * OfferDetail Messages
 *
 * This contains all the text for the OfferDetail container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.OfferDetail";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the OfferDetail container!"
  }
});
