/*
 * Payout Messages
 *
 * This contains all the text for the Payout container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Payout";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Payout container!"
  }
});
