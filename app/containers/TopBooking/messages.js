/*
 * TopBooking Messages
 *
 * This contains all the text for the TopBooking container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.TopBooking";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the TopBooking container!"
  }
});
