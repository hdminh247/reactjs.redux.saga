/*
 * CarManagement Messages
 *
 * This contains all the text for the CarManagement container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.CarManagement";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CarManagement container!"
  }
});
