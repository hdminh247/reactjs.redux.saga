/*
 * ProfileInfo Messages
 *
 * This contains all the text for the ProfileInfo container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ProfileInfo";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ProfileInfo container!"
  }
});
