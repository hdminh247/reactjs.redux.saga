/*
 * StepOneSignUp Messages
 *
 * This contains all the text for the StepOneSignUp component.
 */

import { defineMessages } from "react-intl";

export default defineMessages({
  header: {
    id: "app.components.StepOneSignUp.header",
    defaultMessage: "This is the StepOneSignUp component !"
  }
});
