/*
 * TopBookingItem Messages
 *
 * This contains all the text for the TopBookingItem component.
 */

import { defineMessages } from "react-intl";

export default defineMessages({
  header: {
    id: "app.components.TopBookingItem.header",
    defaultMessage: "This is the TopBookingItem component !"
  }
});
