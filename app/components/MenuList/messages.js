/*
 * MenuList Messages
 *
 * This contains all the text for the MenuList component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.MenuList";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the MenuList component!"
  }
});
