/*
 * BookingDetailInfo Messages
 *
 * This contains all the text for the BookingDetailInfo component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.BookingDetailInfo";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BookingDetailInfo component!"
  }
});
