/*
 * DestinationDetailInfo Messages
 *
 * This contains all the text for the DestinationDetailInfo component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.DestinationDetailInfo";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DestinationDetailInfo component!"
  }
});
