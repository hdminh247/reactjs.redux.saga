/*
 * DriverWithRatingBox Messages
 *
 * This contains all the text for the DriverWithRatingBox component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.DriverWithRatingBox";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DriverWithRatingBox component!"
  }
});
