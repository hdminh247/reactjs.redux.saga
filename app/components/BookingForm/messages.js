/*
 * BookingForm Messages
 *
 * This contains all the text for the BookingForm component.
 */

import { defineMessages } from "react-intl";

export default defineMessages({
  header: {
    id: "app.components.BookingForm.header",
    defaultMessage: "This is the BookingForm component !"
  }
});
