/*
 * AccountBankDetail Messages
 *
 * This contains all the text for the AccountBankDetail component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.AccountBankDetail";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the AccountBankDetail component!"
  }
});
