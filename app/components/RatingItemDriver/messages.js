/*
 * RatingItemDriver Messages
 *
 * This contains all the text for the RatingItemDriver component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.RatingItemDriver";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the RatingItemDriver component!"
  }
});
