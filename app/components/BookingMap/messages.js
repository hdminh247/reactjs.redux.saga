/*
 * BookingMap Messages
 *
 * This contains all the text for the BookingMap component.
 */

import { defineMessages } from "react-intl";

export default defineMessages({
  header: {
    id: "app.components.BookingMap.header",
    defaultMessage: "This is the BookingMap component !"
  }
});
