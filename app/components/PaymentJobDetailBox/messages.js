/*
 * PaymentJobDetailBox Messages
 *
 * This contains all the text for the PaymentJobDetailBox component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.PaymentJobDetailBox";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PaymentJobDetailBox component!"
  }
});
