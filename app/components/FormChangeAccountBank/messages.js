/*
 * FormChangeAccountBank Messages
 *
 * This contains all the text for the FormChangeAccountBank component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.FormChangeAccountBank";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the FormChangeAccountBank component!"
  }
});
