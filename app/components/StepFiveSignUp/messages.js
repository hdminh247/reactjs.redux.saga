/*
 * StepFiveSignUp Messages
 *
 * This contains all the text for the StepFiveSignUp component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.StepFiveSignUp";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the StepFiveSignUp component!"
  }
});
