/*
 * RecommendRideItem Messages
 *
 * This contains all the text for the RecommendRideItem component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.RecommendRideItem";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the RecommendRideItem component!"
  }
});
