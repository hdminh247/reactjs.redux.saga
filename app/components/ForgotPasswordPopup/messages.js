/*
 * ForgotPasswordPopup Messages
 *
 * This contains all the text for the ForgotPasswordPopup component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.ForgotPasswordPopup";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ForgotPasswordPopup component!"
  }
});
