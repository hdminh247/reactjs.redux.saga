/*
 * FeaturedDestinationsSection Messages
 *
 * This contains all the text for the FeaturedDestinationsSection component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.FeaturedDestinationsSection";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the FeaturedDestinationsSection component!"
  }
});
