/*
 * StepFourSignUp Messages
 *
 * This contains all the text for the StepFourSignUp component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.StepFourSignUp";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the StepFourSignUp component!"
  }
});
