/*
 * SocialLogin Messages
 *
 * This contains all the text for the SocialLogin component.
 */

import { defineMessages } from "react-intl";

export default defineMessages({
  header: {
    id: "app.components.SocialLogin.header",
    defaultMessage: "This is the SocialLogin component !"
  }
});
